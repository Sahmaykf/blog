# 后端配置
DB_DSN=root:password@tcp(127.0.0.1:3306)/blog_system?charset=utf8mb4&parseTime=True&loc=Local
APP_PORT=
UPLOAD_DIR=
JWT_SECRET=

# 前端配置 (Vite)
# 开发环境通常不需要设置，使用代理即可
# 生产环境如果 API 不在同域，可以设置此变量
VITE_API_BASE_URL=
